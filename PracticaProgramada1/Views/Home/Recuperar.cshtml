@model PracticaProgramada1.Models.RecuperarModel

<div class="container-xxl">
    <div class="authentication-wrapper authentication-basic container-p-y">
        <div class="authentication-inner py-4">
            <!-- Recuperar Contraseña -->
            <div class="card">
                <div class="card-body">
                    <!-- Logo -->
                    <div class="app-brand justify-content-center">
                        <a href="Index" class="app-brand-link gap-2">
                            <span class="app-brand-logo demo">
                                <!-- Aquí va tu SVG del logo -->
                            </span>
                            <a asp-action="Index" asp-controller="Home">
                                <p class="mb-3"><img src="/img/stisla.png" alt="logo" width="50" class="shadow-light rounded-circle"></p>
                            </a>
                        </a>
                    </div>
                    <!-- /Logo -->

                    <h4 class="mb-3">¿Olvidaste tu contraseña? 🔒</h4>
                    <p class="mb-3">Ingresa tu correo electrónico y te enviaremos una contraseña temporal</p>

                    @if (ViewBag.Error != null)
                    {
                        <div class="alert alert-danger">@ViewBag.Error</div>
                    }

                    <form id="formAuthentication" class="mb-3" asp-action="Recuperar" asp-controller="Home" method="post">
                        <div class="mb-3">
                            <label asp-for="Correo" class="form-label">Correo electrónico</label>
                            <input asp-for="Correo"
                                   type="text"
                                   class="form-control"
                                   placeholder="Ingresa tu correo electrónico"
                                   autofocus />
                            <span asp-validation-for="Correo" class="text-danger small"></span>
                        </div>
                        <button type="submit" class="btn btn-primary d-grid w-100">Enviar contraseña temporal</button>
                    </form>

                    <div class="text-center">
                        <a href="Index" class="d-flex align-items-center justify-content-center">
                            <i class="bx bx-chevron-left scaleX-n1-rtl bx-sm"></i>
                            Volver al inicio de sesión
                        </a>
                    </div>
                </div>
            </div>
            <!-- /Recuperar Contraseña -->
        </div>
    </div>
</div>
