@model PracticaProgramada1.Models.CursoModel

@{
    ViewData["Title"] = "Registrar Curso";
}

<div class="container mt-4">

    <div class="card shadow">

        <div class="card-header bg-success text-white">
            <h3 class="mb-0">
                Registrar Curso
            </h3>
        </div>

        <div class="card-body">

            @if (ViewBag.Error != null)
            {
                <div class="alert alert-danger">
                    @ViewBag.Error
                </div>
            }

            <form asp-action="Registrar"
                  method="post">

                <div asp-validation-summary="ModelOnly"
                     class="text-danger mb-3">
                </div>

                <div class="mb-3">

                    <label asp-for="IdProfesor"
                           class="form-label">
                        Profesor
                    </label>

                    <select asp-for="IdProfesor"
                            asp-items="ViewBag.Profesores"
                            class="form-select">

                        <option value="">
                            Seleccione un profesor
                        </option>

                    </select>

                    <span asp-validation-for="IdProfesor"
                          class="text-danger">
                    </span>

                </div>

                <div class="mb-3">

                    <label asp-for="NombreCurso"
                           class="form-label">
                        Nombre del curso
                    </label>

                    <input asp-for="NombreCurso"
                           class="form-control"
                           maxlength="150"
                           placeholder="Ingrese el nombre del curso" />

                    <span asp-validation-for="NombreCurso"
                          class="text-danger">
                    </span>

                </div>

                <div class="mb-3">

                    <label asp-for="Descripcion"
                           class="form-label">
                        Descripción
                    </label>

                    <textarea asp-for="Descripcion"
                              class="form-control"
                              rows="4"
                              maxlength="500"
                              placeholder="Ingrese la descripción del curso"></textarea>

                    <span asp-validation-for="Descripcion"
                          class="text-danger">
                    </span>

                </div>

                <input asp-for="Estado"
                       type="hidden"
                       value="true" />

                <button type="submit"
                        class="btn btn-success">
                    Guardar
                </button>

                <a asp-action="Index"
                   class="btn btn-secondary">
                    Cancelar
                </a>

            </form>

        </div>

    </div>

</div>

@section Scripts
{
    @{
        await Html.RenderPartialAsync(
            "_ValidationScriptsPartial");
    }
}
