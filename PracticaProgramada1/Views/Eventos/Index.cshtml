@model IEnumerable<PracticaProgramada1.Models.EventoModel>

@{
    ViewData["Title"] = "Eventos";
}

<div class="container mt-4">

    <div class="d-flex justify-content-between align-items-center mb-3">

        <h2>Lista de Eventos</h2>

        <a asp-action="Registrar"
           class="btn btn-success">
            Nuevo Evento
        </a>

    </div>

    @if (TempData["Mensaje"] != null)
    {
        <div class="alert alert-success">
            @TempData["Mensaje"]
        </div>
    }

    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger">
            @TempData["Error"]
        </div>
    }

    @if (ViewBag.Error != null)
    {
        <div class="alert alert-danger">
            @ViewBag.Error
        </div>
    }

    @if (Model == null || !Model.Any())
    {
        <div class="alert alert-info">
            No hay eventos registrados.
        </div>
    }
    else
    {
        <div class="table-responsive">

            <table class="table table-bordered table-hover">

                <thead class="table-dark">
                    <tr>
                        <th>ID</th>
                        <th>Curso</th>
                        <th>Título</th>
                        <th>Fecha</th>
                        <th>Lugar</th>
                        <th>Estado</th>
                        <th>Acciones</th>
                    </tr>
                </thead>

                <tbody>

                    @foreach (var item in Model)
                    {
                        <tr>

                            <td>@item.IdEvento</td>

                            <td>
                                @if (!string.IsNullOrWhiteSpace(item.NombreCurso))
                                {
                                    @item.NombreCurso
                                }
                                else
                                {
                                    <span class="text-muted">
                                        Curso #@item.IdCurso
                                    </span>
                                }
                            </td>

                            <td>@item.Titulo</td>

                            <td>
                                @item.FechaEvento.ToString("dd/MM/yyyy HH:mm")
                            </td>

                            <td>@item.Lugar</td>

                            <td>
                                @if (item.Estado)
                                {
                                    <span class="badge bg-success">
                                        Activo
                                    </span>
                                }
                                else
                                {
                                    <span class="badge bg-danger">
                                        Inactivo
                                    </span>
                                }
                            </td>

                            <td>

                                <a asp-action="Detalle"
                                   asp-route-id="@item.IdEvento"
                                   class="btn btn-info btn-sm">
                                    Detalle
                                </a>

                                <a asp-action="Editar"
                                   asp-route-id="@item.IdEvento"
                                   class="btn btn-warning btn-sm">
                                    Editar
                                </a>

                                @if (item.Estado)
                                {
                                    <a asp-action="Desactivar"
                                       asp-route-id="@item.IdEvento"
                                       class="btn btn-danger btn-sm"
                                       onclick="return confirm('¿Desea desactivar este evento?');">
                                        Desactivar
                                    </a>
                                }

                            </td>

                        </tr>
                    }

                </tbody>

            </table>

        </div>
    }

</div>